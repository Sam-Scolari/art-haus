---
type Props = {
  href?: string;
  outline?: boolean;
  newTab?: boolean;
};

const { href, outline, newTab } = Astro.props;
---

<a
  href={href}
  target={newTab ? "_blank" : ""}
  rel={newTab ? "noopener noreferrer" : ""}
  class={`font-nunito px-8 py-3 text-lg rounded-full ${
    outline
      ? "text-white shadow-[inset_0px_0px_0px_3px_white]"
      : "text-white bg-black"
  }`}
>
  <slot />
</a>
